package stratego;

/**
 * CombatResult is an enum which holds the possible results of an attack.
 */
public enum CombatResult {
    /**
     * win is a win.
     */
    WIN,
    /**
     * draw is draw.
     */
    DRAW,
    /**
     * lose is a loss.
     */
    LOSE;
}
